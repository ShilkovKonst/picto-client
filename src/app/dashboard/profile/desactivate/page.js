import UserProfileDeactivate from '@/_components/dashboard/profile/userProfileDeactivate'
import React from 'react'

const page = ({user}) => {
  return (
    <UserProfileDeactivate user={user} />
  )
}

export default page